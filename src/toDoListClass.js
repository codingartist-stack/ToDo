export default class toDoItem {
  constructor(toDoSection, toDoList, toDoItem, dueDate) {
    this.toDoSection = toDoSection;
    this.toDoList = toDoList;
    this.toDoItem = toDoItem;
    this.dueDate = dueDate;
  }
}
